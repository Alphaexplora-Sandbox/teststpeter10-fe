import { SERVICE_NAME } from '../../src/index';

describe('teststpeter10-frontend', () => {
  it('should export SERVICE_NAME', () => {
    expect(SERVICE_NAME).toBe('teststpeter10-frontend');
  });
});